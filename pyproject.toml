[project]
name = "xactionswrapper"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "fastapi >= 0.141.1",
    "python-dotenv>=1.2.2",
    "uvicorn>=0.52.1",
    "vadersentiment>=3.3.2",
]
